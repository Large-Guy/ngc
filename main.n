module program;

f32[] filter(f32[]& arr, bool filter(f32 x))
{
    return arr;
}

i32 main()
{
    let list = (-2f, 3f, -5f, 7f, 11f, -13f);
    i32 casting = i32 3f;
    let positive = filter(list, bool(f32 x) { return x > 0; });
}